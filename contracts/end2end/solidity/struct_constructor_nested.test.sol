import 'struct_constructor_nested.sol';
import 'Soltest.sol'; // this file will be virtually provided by the testing environment - it's not existing physically.

contract struct_constructor_nested_Test is Soltest { 
    function test_struct_constructor_nested() { 
        // compileAndRun(sourceCode, 0, "C");
        //
        // auto out = encodeArgs(u256(1), u256(0), u256(9), u256(0), u256(4), u256(5));
        // ABI_CHECK(callContractFunction("get()"), out);
    } 
} 

