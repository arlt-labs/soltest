import 'destructuring_assignment.sol';
import 'Soltest.sol'; // this file will be virtually provided by the testing environment - it's not existing physically.

contract destructuring_assignment_Test is Soltest { 
    function test_destructuring_assignment() { 
        // compileAndRun(sourceCode);
        // ABI_CHECK(callContractFunction("f(bytes)", u256(0x20), u256(5), string("abcde")), encodeArgs(u256(0)));
    } 
} 

