import 'inline_assembly_function_access.sol';
import 'Soltest.sol'; // this file will be virtually provided by the testing environment - it's not existing physically.

contract inline_assembly_function_access_Test is Soltest { 
    function test_inline_assembly_function_access() { 
        // compileAndRun(sourceCode, 0, "C");
        // ABI_CHECK(callContractFunction("f(uint256)", u256(5)), encodeArgs());
        // ABI_CHECK(callContractFunction("x()"), encodeArgs(u256(10)));
    } 
} 

