import 'conditional_expression_with_return_values.sol';
import 'Soltest.sol'; // this file will be virtually provided by the testing environment - it's not existing physically.

contract conditional_expression_with_return_values_Test is Soltest { 
    function test_conditional_expression_with_return_values() { 
        // compileAndRun(sourceCode);
        // ABI_CHECK(callContractFunction("f(bool,uint256)", true, u256(20)), encodeArgs(u256(20), u256(0)));
        // ABI_CHECK(callContractFunction("f(bool,uint256)", false, u256(20)), encodeArgs(u256(0), u256(20)));
    } 
} 

