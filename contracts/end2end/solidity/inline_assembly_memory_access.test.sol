import 'inline_assembly_memory_access.sol';
import 'Soltest.sol'; // this file will be virtually provided by the testing environment - it's not existing physically.

contract inline_assembly_memory_access_Test is Soltest { 
    function test_inline_assembly_memory_access() { 
        // compileAndRun(sourceCode, 0, "C");
        // ABI_CHECK(callContractFunction("test()"), encodeArgs(u256(0x20), u256(5), string("12345")));
    } 
} 

