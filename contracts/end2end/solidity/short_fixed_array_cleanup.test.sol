import 'short_fixed_array_cleanup.sol';
import 'Soltest.sol'; // this file will be virtually provided by the testing environment - it's not existing physically.

contract short_fixed_array_cleanup_Test is Soltest { 
    function test_short_fixed_array_cleanup() { 
        // compileAndRun(sourceCode);
        // BOOST_CHECK(storageEmpty(m_contractAddress));
        // ABI_CHECK(callContractFunction("fill()"), bytes());
        // BOOST_CHECK(!storageEmpty(m_contractAddress));
        // ABI_CHECK(callContractFunction("clear()"), bytes());
        // BOOST_CHECK(storageEmpty(m_contractAddress));
    } 
} 

