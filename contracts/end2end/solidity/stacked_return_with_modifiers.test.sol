import 'stacked_return_with_modifiers.sol';
import 'Soltest.sol'; // this file will be virtually provided by the testing environment - it's not existing physically.

contract stacked_return_with_modifiers_Test is Soltest { 
    function test_stacked_return_with_modifiers() { 
        // compileAndRun(sourceCode, 0, "C");
        // ABI_CHECK(callContractFunction("x()"), encodeArgs(u256(0)));
        // ABI_CHECK(callContractFunction("f()"), encodeArgs());
        // ABI_CHECK(callContractFunction("x()"), encodeArgs(u256(1)));
    } 
} 

