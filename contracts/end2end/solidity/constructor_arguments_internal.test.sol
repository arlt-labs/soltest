import 'constructor_arguments_internal.sol';
import 'Soltest.sol'; // this file will be virtually provided by the testing environment - it's not existing physically.

contract constructor_arguments_internal_Test is Soltest { 
    function test_constructor_arguments_internal() { 
        // compileAndRun(sourceCode, 0, "Main");
        // ABI_CHECK(callContractFunction("getFlag()"), encodeArgs(true));
        // ABI_CHECK(callContractFunction("getName()"), encodeArgs("abc"));
    } 
} 

