import 'overloaded_function_call_resolve_to_second.sol';
import 'Soltest.sol'; // this file will be virtually provided by the testing environment - it's not existing physically.

contract overloaded_function_call_resolve_to_second_Test is Soltest { 
    function test_overloaded_function_call_resolve_to_second() { 
        // compileAndRun(sourceCode);
        // ABI_CHECK(callContractFunction("g()"), encodeArgs(10));
    } 
} 

