import 'function_modifier_multiple_times.sol';
import 'Soltest.sol'; // this file will be virtually provided by the testing environment - it's not existing physically.

contract function_modifier_multiple_times_Test is Soltest { 
    function test_function_modifier_multiple_times() { 
        // compileAndRun(sourceCode);
        // ABI_CHECK(callContractFunction("f(uint256)", u256(3)), encodeArgs(2 + 5 + 3));
        // ABI_CHECK(callContractFunction("a()"), encodeArgs(2 + 5 + 3));
    } 
} 

