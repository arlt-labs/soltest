import 'conditional_expression_functions.sol';
import 'Soltest.sol'; // this file will be virtually provided by the testing environment - it's not existing physically.

contract conditional_expression_functions_Test is Soltest { 
    function test_conditional_expression_functions() { 
        // compileAndRun(sourceCode);
        // ABI_CHECK(callContractFunction("f(bool)", true), encodeArgs(u256(1)));
        // ABI_CHECK(callContractFunction("f(bool)", false), encodeArgs(u256(2)));
    } 
} 

