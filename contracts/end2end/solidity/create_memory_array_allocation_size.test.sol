import 'create_memory_array_allocation_size.sol';
import 'Soltest.sol'; // this file will be virtually provided by the testing environment - it's not existing physically.

contract create_memory_array_allocation_size_Test is Soltest { 
    function test_create_memory_array_allocation_size() { 
        // compileAndRun(sourceCode);
        // ABI_CHECK(callContractFunction("f()"), encodeArgs(0x40, 0x40, 0x20 + 256));
    } 
} 

