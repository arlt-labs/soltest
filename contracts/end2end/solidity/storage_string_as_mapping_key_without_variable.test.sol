import 'storage_string_as_mapping_key_without_variable.sol';
import 'Soltest.sol'; // this file will be virtually provided by the testing environment - it's not existing physically.

contract storage_string_as_mapping_key_without_variable_Test is Soltest { 
    function test_storage_string_as_mapping_key_without_variable() { 
        // compileAndRun(sourceCode, 0, "Test");
        // ABI_CHECK(callContractFunction("f()"), encodeArgs(u256(2)));
    } 
} 

