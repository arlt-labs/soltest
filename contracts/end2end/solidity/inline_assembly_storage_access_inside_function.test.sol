import 'inline_assembly_storage_access_inside_function.sol';
import 'Soltest.sol'; // this file will be virtually provided by the testing environment - it's not existing physically.

contract inline_assembly_storage_access_inside_function_Test is Soltest { 
    function test_inline_assembly_storage_access_inside_function() { 
        // compileAndRun(sourceCode, 0, "C");
        // ABI_CHECK(callContractFunction("f()"), encodeArgs(true));
        // ABI_CHECK(callContractFunction("z()"), encodeArgs(u256(7)));
    } 
} 

