import 'assignment_to_const_var_involving_expression.sol';
import 'Soltest.sol'; // this file will be virtually provided by the testing environment - it's not existing physically.

contract assignment_to_const_var_involving_expression_Test is Soltest { 
    function test_assignment_to_const_var_involving_expression() { 
        // compileAndRun(sourceCode);
        // ABI_CHECK(callContractFunction("f()"), encodeArgs(0x123 + 0x456 + 1));
    } 
} 

