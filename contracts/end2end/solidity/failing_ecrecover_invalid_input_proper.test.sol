import 'failing_ecrecover_invalid_input_proper.sol';
import 'Soltest.sol'; // this file will be virtually provided by the testing environment - it's not existing physically.

contract failing_ecrecover_invalid_input_proper_Test is Soltest { 
    function test_failing_ecrecover_invalid_input_proper() { 
        // compileAndRun(sourceCode, 0, "C");
        // ABI_CHECK(callContractFunction("f()"), encodeArgs(u256(0)));
    } 
} 

