import 'fixed_bytes_length_access.sol';
import 'Soltest.sol'; // this file will be virtually provided by the testing environment - it's not existing physically.

contract fixed_bytes_length_access_Test is Soltest { 
    function test_fixed_bytes_length_access() { 
        // compileAndRun(sourceCode, 0, "C");
        // ABI_CHECK(callContractFunction("f(bytes32)", "789"), encodeArgs(u256(32), u256(16), u256(8)));
    } 
} 

