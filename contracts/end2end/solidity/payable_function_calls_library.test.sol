import 'payable_function_calls_library.sol';
import 'Soltest.sol'; // this file will be virtually provided by the testing environment - it's not existing physically.

contract payable_function_calls_library_Test is Soltest { 
    function test_payable_function_calls_library() { 
        // compileAndRun(sourceCode, 0, "L");
        // compileAndRun(sourceCode, 0, "C", bytes(), map<string, Address>{{"L", m_contractAddress}});
        // ABI_CHECK(callContractFunctionWithValue("f()", 27), encodeArgs(u256(7)));
    } 
} 

