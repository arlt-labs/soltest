import 'inline_assembly_storage_access_via_pointer.sol';
import 'Soltest.sol'; // this file will be virtually provided by the testing environment - it's not existing physically.

contract inline_assembly_storage_access_via_pointer_Test is Soltest { 
    function test_inline_assembly_storage_access_via_pointer() { 
        // compileAndRun(sourceCode, 0, "C");
        // ABI_CHECK(callContractFunction("f()"), encodeArgs(true));
        // ABI_CHECK(callContractFunction("a()"), encodeArgs(u256(7)));
        // ABI_CHECK(callContractFunction("separator()"), encodeArgs(u256(0)));
        // ABI_CHECK(callContractFunction("separator2()"), encodeArgs(u256(0)));
    } 
} 

