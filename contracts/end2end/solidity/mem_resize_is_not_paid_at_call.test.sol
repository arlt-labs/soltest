import 'mem_resize_is_not_paid_at_call.sol';
import 'Soltest.sol'; // this file will be virtually provided by the testing environment - it's not existing physically.

contract mem_resize_is_not_paid_at_call_Test is Soltest { 
    function test_mem_resize_is_not_paid_at_call() { 
        //
        // compileAndRun(sourceCode, 0, "C");
        // u160 cAddr = m_contractAddress;
        // compileAndRun(sourceCode, 0, "D");
        // ABI_CHECK(callContractFunction("f(address)", cAddr), encodeArgs(u256(7)));
    } 
} 

