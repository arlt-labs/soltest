import 'keccak256_multiple_arguments.sol';
import 'Soltest.sol'; // this file will be virtually provided by the testing environment - it's not existing physically.

contract keccak256_multiple_arguments_Test is Soltest { 
    function test_keccak256_multiple_arguments() { 
        // compileAndRun(sourceCode);
        //
        // ABI_CHECK(callContractFunction("foo(uint256,uint256,uint256)", 10, 12, 13), encodeArgs(
        // 	dev::keccak256(
        // 		toBigEndian(u256(10)) +
        // 		toBigEndian(u256(12)) +
        // 		toBigEndian(u256(13))
        // 	)
        // ));
    } 
} 

