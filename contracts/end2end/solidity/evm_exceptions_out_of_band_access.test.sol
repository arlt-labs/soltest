import 'evm_exceptions_out_of_band_access.sol';
import 'Soltest.sol'; // this file will be virtually provided by the testing environment - it's not existing physically.

contract evm_exceptions_out_of_band_access_Test is Soltest { 
    function test_evm_exceptions_out_of_band_access() { 
        // compileAndRun(sourceCode, 0, "A");
        // ABI_CHECK(callContractFunction("test()"), encodeArgs(false));
        // ABI_CHECK(callContractFunction("testIt()"), encodeArgs());
        // ABI_CHECK(callContractFunction("test()"), encodeArgs(false));
    } 
} 

