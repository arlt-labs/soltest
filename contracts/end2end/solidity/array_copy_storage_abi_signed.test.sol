import 'array_copy_storage_abi_signed.sol';
import 'Soltest.sol'; // this file will be virtually provided by the testing environment - it's not existing physically.

contract array_copy_storage_abi_signed_Test is Soltest { 
    function test_array_copy_storage_abi_signed() { 
        // compileAndRun(sourceCode);
        // bytes valueSequence;
        // ABI_CHECK(callContractFunction("test()"), encodeArgs(0x20, 8,
        // 	u256(-1),
        // 	u256(-1),
        // 	u256(8),
        // 	u256(-16),
        // 	u256(-2),
        // 	u256(6),
        // 	u256(8),
        // 	u256(-1)
        // ));
    } 
} 

