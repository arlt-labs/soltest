import 'conditional_expression_tuples.sol';
import 'Soltest.sol'; // this file will be virtually provided by the testing environment - it's not existing physically.

contract conditional_expression_tuples_Test is Soltest { 
    function test_conditional_expression_tuples() { 
        // compileAndRun(sourceCode);
        // ABI_CHECK(callContractFunction("f(bool)", true), encodeArgs(u256(1), u256(2)));
        // ABI_CHECK(callContractFunction("f(bool)", false), encodeArgs(u256(3), u256(4)));
    } 
} 

