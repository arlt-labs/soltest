import 'function_modifier_local_variables.sol';
import 'Soltest.sol'; // this file will be virtually provided by the testing environment - it's not existing physically.

contract function_modifier_local_variables_Test is Soltest { 
    function test_function_modifier_local_variables() { 
        // compileAndRun(sourceCode);
        // ABI_CHECK(callContractFunction("f(bool)", true), encodeArgs(0));
        // ABI_CHECK(callContractFunction("f(bool)", false), encodeArgs(3));
    } 
} 

