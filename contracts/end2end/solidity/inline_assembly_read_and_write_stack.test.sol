import 'inline_assembly_read_and_write_stack.sol';
import 'Soltest.sol'; // this file will be virtually provided by the testing environment - it's not existing physically.

contract inline_assembly_read_and_write_stack_Test is Soltest { 
    function test_inline_assembly_read_and_write_stack() { 
        // compileAndRun(sourceCode, 0, "C");
        // ABI_CHECK(callContractFunction("f()"), encodeArgs(u256(45)));
    } 
} 

