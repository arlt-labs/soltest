import 'conditional_expression_true_literal.sol';
import 'Soltest.sol'; // this file will be virtually provided by the testing environment - it's not existing physically.

contract conditional_expression_true_literal_Test is Soltest { 
    function test_conditional_expression_true_literal() { 
        // compileAndRun(sourceCode);
        // ABI_CHECK(callContractFunction("f()", bytes()), toBigEndian(u256(5)));
    } 
} 

