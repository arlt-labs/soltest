import 'copy_internal_function_array_to_storage.sol';
import 'Soltest.sol'; // this file will be virtually provided by the testing environment - it's not existing physically.

contract copy_internal_function_array_to_storage_Test is Soltest { 
    function test_copy_internal_function_array_to_storage() { 
        //
        // compileAndRun(sourceCode, 0, "C");
        // ABI_CHECK(callContractFunction("one()"), encodeArgs(u256(3)));
        // ABI_CHECK(callContractFunction("two()"), encodeArgs());
    } 
} 

