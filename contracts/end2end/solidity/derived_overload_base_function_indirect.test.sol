import 'derived_overload_base_function_indirect.sol';
import 'Soltest.sol'; // this file will be virtually provided by the testing environment - it's not existing physically.

contract derived_overload_base_function_indirect_Test is Soltest { 
    function test_derived_overload_base_function_indirect() { 
        // compileAndRun(sourceCode, 0, "C");
        // ABI_CHECK(callContractFunction("g()"), encodeArgs(10));
        // ABI_CHECK(callContractFunction("h()"), encodeArgs(2));
    } 
} 

