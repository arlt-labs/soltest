import 'using_contract_enums_with_explicit_contract_name.sol';
import 'Soltest.sol'; // this file will be virtually provided by the testing environment - it's not existing physically.

contract using_contract_enums_with_explicit_contract_name_Test is Soltest { 
    function test_using_contract_enums_with_explicit_contract_name() { 
        // compileAndRun(sourceCode);
        // ABI_CHECK(callContractFunction("answer()"), encodeArgs(1));
    } 
} 

