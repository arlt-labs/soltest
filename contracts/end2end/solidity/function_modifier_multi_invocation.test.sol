import 'function_modifier_multi_invocation.sol';
import 'Soltest.sol'; // this file will be virtually provided by the testing environment - it's not existing physically.

contract function_modifier_multi_invocation_Test is Soltest { 
    function test_function_modifier_multi_invocation() { 
        // compileAndRun(sourceCode);
        // ABI_CHECK(callContractFunction("f(bool)", false), encodeArgs(1));
        // ABI_CHECK(callContractFunction("f(bool)", true), encodeArgs(2));
    } 
} 

