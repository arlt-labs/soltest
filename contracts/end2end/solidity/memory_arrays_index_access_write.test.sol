import 'memory_arrays_index_access_write.sol';
import 'Soltest.sol'; // this file will be virtually provided by the testing environment - it's not existing physically.

contract memory_arrays_index_access_write_Test is Soltest { 
    function test_memory_arrays_index_access_write() { 
        // compileAndRun(sourceCode, 0, "Test");
        //
        // vector<u256> data(3 * 4);
        // data[3 * 2 + 2] = 1;
        // data[3 * 3 + 2] = 7;
        // ABI_CHECK(callContractFunction("f()"), encodeArgs(data));
    } 
} 

