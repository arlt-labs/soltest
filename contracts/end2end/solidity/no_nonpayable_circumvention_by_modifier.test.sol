import 'no_nonpayable_circumvention_by_modifier.sol';
import 'Soltest.sol'; // this file will be virtually provided by the testing environment - it's not existing physically.

contract no_nonpayable_circumvention_by_modifier_Test is Soltest { 
    function test_no_nonpayable_circumvention_by_modifier() { 
        // compileAndRun(sourceCode);
        // ABI_CHECK(callContractFunctionWithValue("f()", 27), encodeArgs());
        // BOOST_CHECK_EQUAL(balanceAt(m_contractAddress), 0);
    } 
} 

