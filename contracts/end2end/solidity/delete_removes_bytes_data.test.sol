import 'delete_removes_bytes_data.sol';
import 'Soltest.sol'; // this file will be virtually provided by the testing environment - it's not existing physically.

contract delete_removes_bytes_data_Test is Soltest { 
    function test_delete_removes_bytes_data() { 
        // compileAndRun(sourceCode);
        // ABI_CHECK(callContractFunction("---", 7), bytes());
        // BOOST_CHECK(!storageEmpty(m_contractAddress));
        // ABI_CHECK(callContractFunction("del()", 7), encodeArgs(true));
        // BOOST_CHECK(storageEmpty(m_contractAddress));
    } 
} 

