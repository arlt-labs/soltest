import 'external_types_in_calls.sol';
import 'Soltest.sol'; // this file will be virtually provided by the testing environment - it's not existing physically.

contract external_types_in_calls_Test is Soltest { 
    function test_external_types_in_calls() { 
        // compileAndRun(sourceCode, 0, "C");
        // ABI_CHECK(callContractFunction("test()"), encodeArgs(u256(9), u256(7)));
        // ABI_CHECK(callContractFunction("t2()"), encodeArgs(u256(9)));
    } 
} 

