import 'convert_fixed_bytes_to_uint_same_min_size.sol';
import 'Soltest.sol'; // this file will be virtually provided by the testing environment - it's not existing physically.

contract convert_fixed_bytes_to_uint_same_min_size_Test is Soltest { 
    function test_convert_fixed_bytes_to_uint_same_min_size() { 
        // compileAndRun(sourceCode);
        // ABI_CHECK(
        // 	callContractFunction("bytesToUint(bytes1)", string("a")),
        // 	encodeArgs(u256("0x61"))
        // );
    } 
} 

