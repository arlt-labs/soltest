import 'inline_member_init_inheritence_without_constructor.sol';
import 'Soltest.sol'; // this file will be virtually provided by the testing environment - it's not existing physically.

contract inline_member_init_inheritence_without_constructor_Test is Soltest { 
    function test_inline_member_init_inheritence_without_constructor() { 
        // compileAndRun(sourceCode);
        // ABI_CHECK(callContractFunction("getBMember()"), encodeArgs(5));
        // ABI_CHECK(callContractFunction("getDMember()"), encodeArgs(6));
    } 
} 

