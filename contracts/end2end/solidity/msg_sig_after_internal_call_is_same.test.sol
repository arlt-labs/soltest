import 'msg_sig_after_internal_call_is_same.sol';
import 'Soltest.sol'; // this file will be virtually provided by the testing environment - it's not existing physically.

contract msg_sig_after_internal_call_is_same_Test is Soltest { 
    function test_msg_sig_after_internal_call_is_same() { 
        // compileAndRun(sourceCode);
        // ABI_CHECK(callContractFunction("foo(uint256)", 0), encodeArgs(asString(FixedHash<4>(dev::keccak256("foo(uint256)")).asBytes())));
    } 
} 

