import 'iterated_keccak256_with_bytes.sol';
import 'Soltest.sol'; // this file will be virtually provided by the testing environment - it's not existing physically.

contract iterated_keccak256_with_bytes_Test is Soltest { 
    function test_iterated_keccak256_with_bytes() { 
        // compileAndRun(sourceCode);
        // ABI_CHECK(callContractFunction("foo()"), encodeArgs(
        // 	u256(dev::keccak256(bytes{'b'} + dev::keccak256("xyz").asBytes() + bytes{'a'}))
        // ));
    } 
} 

