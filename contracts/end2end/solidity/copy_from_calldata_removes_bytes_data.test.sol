import 'copy_from_calldata_removes_bytes_data.sol';
import 'Soltest.sol'; // this file will be virtually provided by the testing environment - it's not existing physically.

contract copy_from_calldata_removes_bytes_data_Test is Soltest { 
    function test_copy_from_calldata_removes_bytes_data() { 
        // compileAndRun(sourceCode);
        // ABI_CHECK(callContractFunction("set()", 1, 2, 3, 4, 5), encodeArgs(true));
        // BOOST_CHECK(!storageEmpty(m_contractAddress));
        // sendMessage(bytes(), false);
        // BOOST_CHECK(m_output == bytes());
        // BOOST_CHECK(storageEmpty(m_contractAddress));
    } 
} 

