import 'conditional_expression_different_types.sol';
import 'Soltest.sol'; // this file will be virtually provided by the testing environment - it's not existing physically.

contract conditional_expression_different_types_Test is Soltest { 
    function test_conditional_expression_different_types() { 
        // compileAndRun(sourceCode);
        // ABI_CHECK(callContractFunction("f(bool)", true), encodeArgs(u256(0xcd)));
        // ABI_CHECK(callContractFunction("f(bool)", false), encodeArgs(u256(0xabab)));
    } 
} 

