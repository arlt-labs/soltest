import 'evm_exceptions_in_constructor_call_fail.sol';
import 'Soltest.sol'; // this file will be virtually provided by the testing environment - it's not existing physically.

contract evm_exceptions_in_constructor_call_fail_Test is Soltest { 
    function test_evm_exceptions_in_constructor_call_fail() { 
        // compileAndRun(sourceCode, 0, "B");
        //
        // ABI_CHECK(callContractFunction("testIt()"), encodeArgs());
        // ABI_CHECK(callContractFunction("test()"), encodeArgs(2));
    } 
} 

